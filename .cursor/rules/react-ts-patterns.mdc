---
description: React/TypeScript 与组件开发规范
globs: "**/*.{ts,tsx}"
alwaysApply: false
---

# React/TypeScript 与组件规范

- 使用函数组件和 Hooks，严格 TypeScript 类型，避免 `any`
- Props 接口明确定义；组件需支持 ref forwarding 时显式声明
- 性能关键组件使用 `memo` 优化
- 遵循 **technical-conventions** 规则与 `.cursor/skills/` 下组件类技能
