---
description: 
alwaysApply: true
---

# 项目上下文与文档

本仓库为 **React + TypeScript + Ant Design** 前端项目。做任何代码或需求相关更改前：

1. **按需获取上下文**：
   - **src 目录与模块结构**：使用 **src-tree** skill（进入 `src` 目录执行 `tree` 命令获取项目目录树）
   - **当前任务 / 进度**：读取 `.cursor/docs/` 下 `tasks.md`、`status.md`
2. **涉及架构或任务时**：先通过 src-tree 弄清目录结构，再结合 tasks / status 动手。
3. **所有实现**：须符合 **technical-conventions** 规则（硬性约束）；细节与步骤按需参考 `.cursor/skills/` 下 **style-standards**、**component-and-stack**、**technical-best-practices**、**react-antd-dev**。

规则在 `.cursor/rules/`，技能在 `.cursor/skills/`，文档在 `.cursor/docs/`。
