---
description: 包管理器约定，安装/管理依赖时触发
globs: "{package.json,yarn.lock}"
alwaysApply: true
---

# 包管理器

- **统一使用 Yarn 1.x** 作为包管理器，禁止使用 npm / pnpm
- 安装依赖：`yarn add <pkg>` / `yarn add -D <pkg>`
- 执行脚本：`yarn dev`、`yarn build`、`yarn lint`
- 遇到 peer dependency 冲突时使用 `yarn --ignore-engines`，不要切换到 npm
- 不要删除或修改 `yarn.lock`，由 Yarn 自动维护
